# Python 3.x (avec Tkinter inclus ou installé séparément via le gestionnaire de paquets système)
# Aucune dépendance PyPI requise

# lancer le main.py pour démarrer l'application
# Assurez-vous que Python est installé et accessible dans votre PATH
# Pour exécuter l'application, utilisez la commande suivante dans le terminal :
# python main.py